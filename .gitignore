# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next
out
next-env.d.ts

# Env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Supabase CLI local cache (project ref, versions, pooler URL) — machine-local,
# rewritten by every CLI command.
supabase/.temp/

# Private business documents / source price files — kept locally only,
# never deployed (not under public/) and never pushed to GitHub.
storage/

# Office temp lock files (e.g. ~$Price List.odt)
~$*
